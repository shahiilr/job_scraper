# Job Scraper Requirements
# 
# This file lists the Python packages required to run the job scraper.
# Install with: pip install -r requirements.txt

# Core Python libraries (these are typically included with Python)
# urllib.parse - for URL encoding (built-in)
# datetime - for timestamp generation (built-in)
# os - for environment variables (built-in)
# sys - for system operations (built-in)

# Optional dependencies for future API integrations:
# requests>=2.28.0        # For HTTP requests to job APIs
# beautifulsoup4>=4.11.0  # For web scraping (if needed)
# lxml>=4.9.0             # XML/HTML parser
# google-generativeai>=0.3.0  # For Gemini AI integration (optional)

# Development dependencies:
# pytest>=7.0.0           # For unit testing
# black>=22.0.0           # Code formatting
# flake8>=5.0.0           # Code linting

# Note: The current simple_job_scraper.py only uses Python standard library modules,
# so no external packages are strictly required for basic functionality.
# Uncomment the packages above as needed for extended features.
